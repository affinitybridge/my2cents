extends ../layout

block title
  h1.title Create new user

block content
  form(action='/user', method='post')!= partial('users/form', {locals: {user: user}})
  p Already have an account? 
      a(href='/login', title="View login page") Log in.